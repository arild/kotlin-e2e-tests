version: '3.8'

services:
  postgres:
    image: postgres:14.2
    environment:
      POSTGRES_USER: my_user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: accounting
    ports:
      - "5500:5432"
